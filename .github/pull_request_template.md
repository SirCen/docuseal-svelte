## What does this change?

<!-- A short description, and the issue it closes if there is one. -->

## Checklist

- [ ] `npm run check`, `npm test` and `npm run build` pass locally
- [ ] Added a changeset (`npx changeset`), or this change doesn't affect the published package
- [ ] Added or updated tests for the behaviour that changed
